@model IEnumerable<varastonhallinta1.Models.TuoteLuokittelu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LuokitteluID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TuoteLuokka)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TuoteLuokka)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LuokitteluID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LuokitteluID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LuokitteluID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LuokitteluID })
        </td>
    </tr>
}

</table>
