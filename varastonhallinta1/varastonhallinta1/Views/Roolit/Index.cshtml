@model IEnumerable<varastonhallinta1.Models.Roolit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Roolinimi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Roolinimi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RooliID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RooliID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RooliID })
        </td>
    </tr>
}

</table>
